# This env file uses @env-spec - see https://varlock.dev/env-spec for more info
# @envFlag=APP_ENV
# @defaultRequired=false @defaultSensitive=false
# @generateTypes(lang=ts, path=env.d.ts)
# ----------

# =============================================================================
# SCHEMA DEFINITION WITH DEVELOPMENT DEFAULTS
# =============================================================================
# Environment-specific override values are in .env.development, .env.staging, .env.production

# --- TOP-LEVEL ENVIRONMENT CONFIGURATION ---

# @required
# @description("Application runtime environment - controls which configuration set is loaded")
# @type=enum(development, staging, production)
APP_ENV=development

# @description("Application log level for debugging and monitoring")
# @type(enum)
LOG_LEVEL=debug

# --- STRING CONFIGURATION ---

# @required
# @description("Shows which `.env.<environment>` file the Varlock config was loaded from")
# @type(string)
OVERRIDE_LOADED_FROM=schema

# --- URL CONFIGURATION ---

